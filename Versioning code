// Version code starting this code should keep clients (players) updated so there is 0-1% of combatibility issues.
String (Version) == null //null cus this will be defined later on
String (newVersion) == null // same reason but will be provided by db/server
